server:
  port: 8080

spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
    
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.xml

  jackson:
    serialization:
      write-dates-as-timestamps: false

  mail:
    host: ${SPRING_MAIL_HOST:smtp.gmail.com}
    port: ${SPRING_MAIL_PORT:587}
    username: ${SPRING_MAIL_USERNAME:}
    password: ${SPRING_MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Base application config
app:
  admin:
    username: admin
    # BCrypt hash of "admin123" - generated with BCryptPasswordEncoder strength 10
    password: "$2a$10$WvMRsusv5nfEKqvYobvmDOp9wKuI1mVaD.9hKo4GAi.6Nnu8sodeC"
  writer:
    username: writer
    # BCrypt hash of "writer123"
    password: "$2a$10$hp1V.zV.9YIgVA0VvkmxJ.pc38CF7qbi6rV9xO.NHT/U.zTK6i.Xe"
  super-admin:
    username: ${SUPER_ADMIN_USERNAME:owner}
    password: ${SUPER_ADMIN_PASSWORD:owner123}
