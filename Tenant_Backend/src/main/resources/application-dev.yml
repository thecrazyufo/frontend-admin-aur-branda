spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/software_platform}
    username: ${SPRING_DATASOURCE_USERNAME:admin}
    password: ${SPRING_DATASOURCE_PASSWORD:admin123}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3003,http://localhost:3004,http://localhost:3005}
    allowed-origin-patterns: ${CORS_ALLOWED_ORIGIN_PATTERNS:https://*.thecrazyufo.in,https://thecrazyufo.in,https://*.vercel.app}
  jwt:
    secret: ${JWT_SECRET:7a3f8c2d1e6b9a4f0e5c8b2d7a1e4f9c3b6a0d8e2f5c9b4a7e1d6c3f0b8a5e2}
    expiration-ms: 86400000          # 24 hours (dev-friendly)
    refresh-expiration-ms: 604800000 # 7 days

payment:
  stripe:
    secret-key: ${STRIPE_SECRET_KEY:sk_test_mock_secret_key_12345}
    publishable-key: ${STRIPE_PUBLISHABLE_KEY:pk_test_mock_pub_key_12345}
  paypal:
    client-id: ${PAYPAL_CLIENT_ID:mock_paypal_client_id}
    client-secret: ${PAYPAL_CLIENT_SECRET:mock_paypal_client_secret}
    mode: ${PAYPAL_MODE:sandbox}
